@model BackendTest1.Models.CheckPageModel
@*@using BackendTest1.Models
@{
    Detail[] bodyMods = new Detail()[];
    Detail[] armMods = new Detail[];
    Detail[] headMods = new Detail[];

    for (int i = 0; i < @Model.Robot.Details.Length; i++)
    {
        if (Model.Robot.Details[i].Type == DetailType.ModBody)
            bodyMods.Add(Model.Robot.Details[i]);
        else if (Model.Robot.Details[i].Type == DetailType.ModBody)
            armMods.Add(Model.Robot.Details[i]);
        else if (Model.Robot.Details[i].Type == DetailType.ModBody)
            headMods.Add(Model.Robot.Details[i]);
    }
}*@

<form asp-action="DeleteRobot" class="form-horizontal">
    <div asp-validation-summary="ModelOnly"></div>
    <div class="row">
        <div class="col-12">
            <h2>@Model.Robot.Name</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-8 offset-2">
            <strong>Body type</strong>: @Model.Robot.Details.First(x => x.Type == DetailType.Body).Name (@Model.Robot.Details.First(x => x.Type == DetailType.Body).Price) <br />
            <strong>Body mods</strong>:  <br />
            <ul>

            </ul>
            <strong>Head type</strong>: Head Type 1 (200$) <br />
            <strong>Head mods</strong>:  <br />
            <ul>
                <li>Head mod 1 (50$)</li>
                <li>Head mod 4 (75$)</li>
            </ul>
            <strong>Arms type</strong>: Arms Type 1 (200$) <br />
            <strong>Arms mods</strong>:  <br />
            <ul>
                <li>Arms mod 1 (50$)</li>
            </ul>
            <strong>Total mods</strong>: 5 <br />
            <strong>Total cost</strong>: 1275$<br />
        </div>
    </div>
</form>